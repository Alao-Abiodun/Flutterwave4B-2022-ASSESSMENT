const Reminder = require("../models/fcs.model");
const { successResMsg, errorResMsg } = require("../utils/libs/response");
const AppError = require("../utils/libs/appError");
const catchAsync = require('../utils/libs/catchAsync');


const feeSetup = catchAsync(async (req, res, next) => {
    try {
        
    } catch (error) {
        
    }
})

const computeTransactionFee = catchAsync(async (req, res, next) => {
    try {
        
    } catch (error) {
        
    }
})

module.exports = {feeSetup, computeTransactionFee}